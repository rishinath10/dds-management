# Copy this file to .env and fill in real values.
# Never commit the real .env file to git.

PORT=3300

# Login shown by your browser when opening the app (HTTP Basic Auth).
# Pick a strong password — this protects staff salary data.
ADMIN_USER=admin
ADMIN_PASS=change-this-password
